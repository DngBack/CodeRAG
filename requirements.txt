# Core dependencies for semantic search system
lancedb>=0.5.0
langchain>=0.1.0
langchain-openai>=0.0.5
openai>=1.0.0

# Tree-sitter for code parsing (optional - using simple chunker instead)
# tree-sitter>=0.20.0

# Data processing and utilities
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0

# File and path handling
pathlib2>=2.3.0

# Environment and configuration
python-dotenv>=1.0.0

# Optional: For better code parsing (if using tree-sitter)
# tree-sitter-typescript>=0.20.0

# Development and testing (optional)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0