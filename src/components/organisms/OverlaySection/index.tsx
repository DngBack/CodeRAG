"use client";

const OverlaySection = () => {
  return (
    <div className="fixed inset-0 bg-overlay z-50 flex items-center justify-center w-full h-full"></div>
  );
};

export default OverlaySection;
